import React from 'react'
import { Button,Container} from '@mantine/core';
import HomeFeatures from '@/features/home';
export default function tes() {
  return (
    <Container fluid h={60} bg="var(--mantine-color-blue-light)">
    <HomeFeatures />
    </Container>
  )
}
